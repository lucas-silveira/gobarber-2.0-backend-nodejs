{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "docker",
  "database": "gobarber",
  "entities": [
    "./src/infra/schemas/**/*.schema.ts"
  ],
  "migrations": [
    "./src/infra/migrations/typeorm/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/infra/migrations"
  }
}
