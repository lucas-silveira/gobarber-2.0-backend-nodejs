{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "docker",
  "database": "gobarber",
  "entities": [
    "./src/infra/schemas/typeorm/*.schema.ts"
  ],
  "migrations": [
    "./src/infra/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/infra/migrations"
  }
}
